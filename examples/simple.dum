@internal(param: int) {
    ret param * 2
}

@dumlang(param: int) {
    dec num_digits: int = 0
    while param {
        param = param / 10
        num_digits = num_digits + 1
    }
    ret num_digits
}
